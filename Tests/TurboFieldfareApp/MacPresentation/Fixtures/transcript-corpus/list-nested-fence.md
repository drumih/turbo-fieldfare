1. Outer step

    - Inner step, which owns the listing below it:

        ~~~bash
        cost $a$ per run
        export TARGET="$HOME/$PATH"
        ~~~

    - Next step uses $x$ directly

Then $y$ closes the answer.
